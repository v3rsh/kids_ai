# ===== Core Framework (pybotx) =====
# Установка из GitHub (PyPI версия устарела):
# pip install git+https://github.com/ExpressApp/pybotx.git
pybotx @ git+https://github.com/ExpressApp/pybotx.git

# ===== Web Framework =====
# Starlette вместо FastAPI (совместимость с pydantic v1)
starlette>=0.27.0,<0.28.0
uvicorn>=0.27.0
# httpx, pydantic, loguru — версии управляются через pybotx (pydantic<1.11)

# ===== Database =====
SQLAlchemy>=2.0.0
# Драйвер для PostgreSQL
asyncpg>=0.29.0
# Драйвер для SQLite (fallback для хранения состояний FSM)
aiosqlite>=0.19.0
# Redis (приоритетное хранилище FSM, с C-парсером hiredis)
redis[hiredis]>=5.0.0

# ===== Utilities =====
python-dotenv>=1.0.0
apscheduler>=3.10.0
typing-extensions>=4.5.0
aiofiles>=23.2.0

# ===== Excel и обработка изображений (Безопасные рисунки) =====
# openpyxl — on-demand генерация registry.xlsx / shortlist.xlsx (§25.4)
openpyxl>=3.1.0
# Pillow — генерация превью работ для жюри (§35.3, 1280 px max)
Pillow>=10.0.0

# ===== Runtime =====
greenlet==2.0.2

# ===== Development & Security =====
bandit>=1.7.5
