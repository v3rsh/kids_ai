# ===== Core Framework (pybotx) =====
# Зафиксировано на версии из production-образа; обновление — отдельный шаг с pytest.
pybotx==0.76.3

# ===== Web Framework =====
# Starlette вместо FastAPI (совместимость с pydantic v2 из pybotx 0.76.x)
starlette>=0.27.0,<0.28.0
uvicorn>=0.27.0
# httpx, pydantic, loguru — версии управляются через pybotx

# ===== Database =====
SQLAlchemy>=2.0.0
# Драйвер для PostgreSQL
asyncpg>=0.29.0
# Redis — единственное хранилище FSM (с C-парсером hiredis)
redis[hiredis]>=5.0.0

# ===== Utilities =====
python-dotenv>=1.0.0
apscheduler>=3.10.0
typing-extensions>=4.5.0
aiofiles>=23.2.0

# ===== Validation (шаг анкеты: телефон/email) =====
# phonenumbers — парсинг, валидация и нормализация телефонов (в т.ч. RU → E.164 +7...)
# email-validator — строгая проверка синтаксиса email (без DNS-запросов, check_deliverability=False)
phonenumbers>=8.13.0
email-validator>=2.0.0

# ===== Excel (Безопасные рисунки) =====
# openpyxl — on-demand генерация registry.xlsx / shortlist.xlsx (§25.4)
openpyxl>=3.1.0

# ===== Runtime =====
greenlet==2.0.2

# ===== Development & Security =====
bandit>=1.7.5

# ===== Testing (см. docs/testing.md) =====
pytest>=8.0.0
pytest-asyncio>=0.23.0
